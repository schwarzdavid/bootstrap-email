<!-- OUTER CONTAINER TEMPLATE -->
<table>
	<tbody>
	<tr>
		<td align="center">

			<% if(variables.containerWidthFallback) { %>
				<!--[if (gte mso 9)|(IE)]>
				<table align="center" border="0" cellpadding="0" cellspacing="0">
					<tbody>
					<tr>
						<td width="<%= variables.width %>">
				<![endif]-->
			<% } %>

			<!-- OUTER CONTAINER CONTENT -->
			<%- content %>
			<!-- / OUTER CONTAINER CONTENT -->

			<% if(variables.containerWidthFallback) { %>
				<!--[if (gte mso 9)|(IE)]>
				</td>
				</tr>
				</tbody>
				</table>
				<![endif]-->
			<% } %>

		</td>
	</tr>
	</tbody>
</table>
<!-- / OUTER CONTAINER TEMPLATEA -->
