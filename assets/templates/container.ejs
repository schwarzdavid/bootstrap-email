<table class="<%= classes %>">
	<tbody>
	<tr>
		<td align="center">

			<% if(classes.indexOf('container-fluid') < 0) { %>
			<!--[if (gte mso 9)|(IE)]>
			<table align="center">
				<tbody>
				<tr>
					<td width="630">
			<![endif]-->
			<% } %>

			<table class="container-inner">
				<tbody>
				<tr>
					<td>
						<%- content %>
					</td>
				</tr>
				</tbody>
			</table>

			<% if(classes.indexOf('container-fluid') < 0) { %>
			<!--[if (gte mso 9)|(IE)]>
			</td></tr></tbody></table>
			<![endif]-->
			<% } %>

		</td>
	</tr>
	</tbody>
</table>
